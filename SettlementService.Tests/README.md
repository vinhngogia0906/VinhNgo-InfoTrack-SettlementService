# Settlement Service Tests

xUnit test project for the [Settlement Service Engine](../SettlementService) application.

Tests cover the `BookingController` including valid bookings, time validation, capacity limits, and name validation. Also run automatically via GitHub Actions CI on every push to `main`.

## Running Tests

From the Settlement Service solution in Visual Studio, or from the command line:

```
dotnet test
```
